Переменная b — значение не определено (код не скомпилируется)
Переменная c = -126
Переменная d — значение не определено (код не скомпилируется)
byte b = a + 10; Значение 130 выходит за пределы byte. Присвоение int → byte требует явного приведения типа, если выражение не является константным.
byte d = a + 1; правило то же самое — требуется явное приведение, так как a — не константа
byte c = (byte)(a + 10); Для byte диапазон 256 значений. 130 - 256 = -126.
